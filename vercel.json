{
    "framework": "nextjs",
    "buildCommand": "prisma generate && next build",
    "outputDirectory": ".next",
    "functions": {
        "src/app/api/**/*": {
            "maxDuration": 300
        },
        "src/actions/**/*": {
            "maxDuration": 300
        }
    }
}